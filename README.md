# 3주차 - Gemini Home Screen

- 단일 화면(`HomeScreen`)에서 제목(Text), 이미지(`R.drawable.gemini`), 링크 버튼 구성
- 버튼 클릭 시 `Intent.ACTION_VIEW`로 `https://gemini.google.com/` 열기
- `ComposeLabTheme` 적용, `@Preview` 제공
## 